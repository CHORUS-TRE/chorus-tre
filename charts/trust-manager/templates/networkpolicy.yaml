{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-trust-manager-policy
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chorus-trust-manager.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "chorus-trust-manager.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  {{- if .Values.networkPolicy.ingress }}
  ingress:
  {{- toYaml .Values.networkPolicy.ingress | nindent 4 }}
  {{- else }}
  {{- /* Default deny: empty ingress list means deny all unless explicitly allowed */}}
  ingress: []
  {{- end }}
  egress:
  {{- with .Values.networkPolicy.egress }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if .Values.networkPolicy.dnsEgress }}
    {{- /* Allow DNS resolution to CoreDNS in .Values.networkPolicy.dnsNamespace
           (default kube-system). */}}
    - to:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: {{ .Values.networkPolicy.dnsNamespace }}
        podSelector:
          matchLabels:
            k8s-app: kube-dns
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: {{ .Values.networkPolicy.dnsNamespace }}
        podSelector:
          matchLabels:
            app: coredns
      ports:
      - protocol: UDP
        port: 53
      - protocol: TCP
        port: 53
  {{- end }}
{{- end }}
