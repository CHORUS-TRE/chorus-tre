{{- $currentScope := . }}
{{ range $path, $_ :=  .Files.Glob  "files/*-realm.json" }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ base $path | replace ".json" "" }}-config
  namespace: {{ $currentScope.Release.Namespace }}
data:
  {{ base $path }}: |-
  {{- with $currentScope }}
    {{ .Files.Get $path | nindent 4 }}
  {{- end }}
{{- end }}
