apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-ci.fullname" . }}
  labels:
    {{- include "argo-ci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "argo-ci.selectorLabels" . | nindent 4 }}
