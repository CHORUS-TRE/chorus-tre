apiVersion: v2
name: harbor
description: Harbor container registry
version: 0.0.1
dependencies:
  - name: harbor
    version: 23.0.2 # if we get a merge to this version
    repository: https://charts.bitnami.com/bitnami
