apiVersion: v1
kind: Service
metadata:
  name: {{ include "i2b2.fullname" . }}
  labels:
    {{- include "i2b2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "i2b2.selectorLabels" . | nindent 4 }}
