apiVersion: v1
kind: Service
metadata:
  name: {{ include "xpra-server.fullname" . }}
  labels:
    {{- include "xpra-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "xpra-server.selectorLabels" . | nindent 4 }}
