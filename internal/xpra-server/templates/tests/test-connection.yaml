apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xpra-server.fullname" . }}-test-connection"
  labels:
    {{- include "xpra-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xpra-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
